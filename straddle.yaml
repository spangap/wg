# wg — WireGuard tunnel for spangap.
#
# Vendors trombik/esp_wireguard with the IDF 5.x netif->state PPP workaround
# (drops out once upstream propagates).
schema_version: 1
name: spangap/wg
prefix: wg
version: 0.1.0
firmware: esp-idf
browser: browser
platforms: [idf]
requires:
  - spangap/spangap-net
optional_requires:
  # wg.cpp registers a Settings pane via lcdSetting* helpers,
  # gated on CONFIG_SPANGAP_LCD.
  - spangap/spangap-lcd
# Auto-init: spangap-inside folds this into the buildable's generated
# spangapInitStraddles() dispatcher, so wg comes up with no consumer main.cpp
# edit. wgInit() only registers net-event callbacks + a CLI verb + storage
# defaults (and an LCD settings pane when lcd is staged) — order-independent
# once net/storage/cli are up, all true at the dispatcher's call site.
init:
  - call: wgInit
